# image:
# clean_keep: 30
# ecr_keep: 30

# aws_profile_vince_env_map:
#   default: prod
#   # More examples:
#   aws_profile1: prod
#   aws_profile2: stag
#   aws_profile3: dev

# vince_env_cluster_map:
#   default: prod
#   # More examples:
#   aws_profile1: prod
#   aws_profile2: stag
#   aws_profile3: dev

# defaults when an new ECS service is created by vince ship
new_service:
  maximum_percent: 200
  minimum_healthy_percent: 100
  desired_count: 1
